generator umlclass

{GraphObject;GraphObject
  label;+string
  name;+string
  color_;+string
  textcolor;+string
  url;+string
}

{GraphConnectable;GraphConnectable
  noedges;+boolean
  edgelabel;+string
}

{GraphContainer;GraphContainer
  OBJECTS;+List<GraphConnectable>
  ROOTVERTICES;+List<GraphConnectable>
  equal_;+List<GraphConnectable>
}

{GraphGroup;GraphGroup
  name_;+string
  exitvertex;+string
  entryedge;+string
  entrance;+GraphConnectable|GraphConnectable[]
  setDefault;+setDefault(key:string,value:string):return
}

{GraphInner;GraphInner
  name__;+string
  isInnerGraph;+boolean=true
  entrance_;+GraphConnectable|GraphConnectable[]
  exit;+GraphConnectable
}

{GraphEdge;GraphEdge
  edgeType;+string
  left;+GraphConnectable
  rigth;+GraphConnectable
  lcompass;+string
  rcompass;+string
  edgetextcolor;+string
  edgecolor;+string
  container;+GraphConnectable
  isDotted;+()
  isDashed;+()
  isBroken;+()
  isBidirectional;+()
  isUndirected;+()
  isLeftPointingEdge;+()
  isRightPointingEdge;+()
}

{GraphVertex;GraphVertex
  name___;+string
  shape_;+string
  image;+string
  style;+string
}

{GraphCanvas;GraphCanvas
  EDGES;+List<GraphEdge>
  generator_;+string
  visualizer_;+string
  shape__;+string
  direction;+string
  start_;+string
  parseError;+(str:string,hash:string)
  result;+(data:string)
}

GraphInner>GraphGroup>GraphContainer>GraphConnectable>GraphObject
GraphCanvas>GraphContainer
GraphVertex>GraphConnectable
GraphEdge>GraphObject
